name: lit_data
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - matplotlib>=3.7.0
  - numpy>=1.24.0
  - pandas>=2.0.0
  - pillow>=10.0.0
  - tk  # Required for TkAgg backend (interactive windows)
  - pip
  - pip:
    - opencv-python>=4.8.0  # For additional image processing if needed

